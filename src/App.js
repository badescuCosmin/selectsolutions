import Hero from './components/Hero';
import AOS from 'aos';
import 'aos/dist/aos.css'; // You can also use <link> for styles
// ..
AOS.init();

function App() {
  return (
    <Hero />
  );
}

export default App;
